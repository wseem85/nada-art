import styled from "styled-components";

const StyledHeader = styled.h1`
  background-color: var(--color-brand-50);
`;
export default function About() {
  return <StyledHeader>About</StyledHeader>;
}
